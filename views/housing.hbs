---
name: housing
type: housing
title: Housing | Career Center
layout: no-social.hbs
---

<section class="content extra-content pathways-content">
  <h2>Housing</h2>
  <p>Need a place to stay during your internship? Here are some intern-friendly homesteads.</p>
  <p class="muted">Email us at <a href="mailto:aucc@auburn.edu">aucc@auburn.edu</a> if you find any broken or outdated links.</p>
</section>

<div class="regions">
  {{#each housing.region}}
    <div class="region">
      <div class="hero">
        {{#if listings}}
          <div class="region-header hero-content">
            <h2>{{ fullname }}</h2>
          </div>
        {{else}}
          <div class="region-header hero-content coming-soon">
            <h2>{{ fullname }}</h2>
          </div>
        {{/if}}
      </div>
      {{#if listings}}
        <section class="content region-content">
          <a class="collapsed accordion-header" data-toggle="collapse" id="{{ name }}-heading" href="#{{ name }}" role="tab" aria-expanded="false" aria-controls="{{ name }}">
            <i></i>
          </a>
          <div style="height: 0px;" aria-expanded="false" id="{{ name }}" class="panel-collapse collapse" role="tabpanel" aria-labelledby="{{ name }}-heading">
            {{#each listings}}
              {{#if link}}
                <a class="listing" href="{{ link }}">{{ name }}<span class="muted"> {{ location }}</span></a>
              {{else}}
                <a class="listing coming-soon" disabled="disabled">{{ name }} <span class="coming-soon">(Coming soon)</span></a>
              {{/if}}
            {{/each}}
          </div>
        </section>
      {{else}}
        <section class="content coming-soon">
          <h2>Coming soon</h2>
        </section>
      {{/if}}
    </div>
  {{/each}}
</div>